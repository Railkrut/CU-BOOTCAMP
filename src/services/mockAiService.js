const wait = (ms) => new Promise((resolve) => setTimeout(resolve, ms));

export const generatePitchRiskAnalysis = async () => {
  // Имитация ответа AI-наставника без реального API.
  await wait(1000);
  return {
    aiReply:
      "Отличный старт. Риски для школьно-студенческого проекта: размытая целевая аудитория, перегруз команды учебой и отсутствие быстрого MVP-теста. Как вы проверите гипотезу за 7 дней?",
    identifiedRisks: "Нечеткая ЦА, нехватка времени у команды, отсутствие быстрых тестов.",
    activity: "Анализ обновлен: добавлен план MVP-проверки для учебной команды",
  };
};

export const generatePitchFollowUp = async () => {
  // Имитация следующего сообщения AI после ответа пользователя.
  await wait(900);
  return "Классно. Добавьте критерии успеха (например, 20 интервью, 30 регистраций, 5 первых оплат) и распределите задачи между участниками команды на неделю.";
};

export const evaluateCaseSolution = async () => {
  // Имитация оценки решения кейса AI.
  await wait(1200);
  return true;
};
